spring:
  application:
    name: account
  cloud:
    config:
      uri: ${CONFIG_HOME}
      fail-fast: true

#spring:
#  kafka:
#    bootstrap-servers: ${KAFKA_URL}
#    topic:
#      helloworld: account.t
